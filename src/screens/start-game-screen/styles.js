import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  title: {
    textAlignVertical: 'center',
    textAlign: 'center',
    fontSize: 32,
    marginBottom: 32,
  },
  subtitle: {
    textAlignVertical: 'center',
    textAlign: 'center',
    fontSize: 16,
    marginBottom: 8,
  },
});
